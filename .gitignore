# Servidor local de desenvolvimento (não vai para produção)
_serve.ps1

# Config interna do Claude Code
.claude/

# Documentação de setup (não faz parte do site)
supabase_setup.sql

# Edge Functions do Supabase (vivem no painel, têm segredo — não publicar)
supabase/

# Sistema
Thumbs.db
.DS_Store
